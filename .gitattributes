# Normalise line endings: LF in the repo, native in the working copy.
# Without this, editing on Windows and deploying to a Linux host churns
# every text file in the diff.
* text=auto eol=lf

# Binary — never touch these
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.pdf  binary
*.ico  binary
